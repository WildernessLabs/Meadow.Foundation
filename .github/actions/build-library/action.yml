name: build-library
description: Builds and publishes M.F. libraries and drivers

inputs:
  packageId:
    required: true
  path:
    required: true
  version:
    required: true
  token:
    required: true

runs:
  using: "composite"
  steps:
    - name: Build the library/driver
      shell: bash
      run: dotnet build -c Source/Meadow.Foundation.Libraries_and_Frameworks/Graphics.MicroGraphics/Driver/Graphics.MicroGraphics.csproj /p:Version=${{ inputs.version }}
    - name: Package the library/driver
      shell: bash
      run: dotnet pack -c Source/Meadow.Foundation.Libraries_and_Frameworks/Graphics.MicroGraphics/Driver/Graphics.MicroGraphics.csproj /p:Version=${{ inputs.version }} --output .
    - name: Push the library/driver to NuGet
      shell: bash
      run: dotnet nuget push ${{ inputs.packageId }}.${{ inputs.version }}.nupkg -s https://api.nuget.org/v3/index.json -k ${{ inputs.token }}
